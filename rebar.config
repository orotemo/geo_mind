{relx, [{release, {geo_mind_release, "0.1.0"},
  [geo_mind]},

  {dev_mode, false}]}.

{deps, [
  lager,
  hackney,
  {geodata2_lib,
    {git, "https://github.com/orotemo/geodata2_lib",
      {branch, "master"}
    }
  }
]}.

{erl_opts, [{parse_transform, lager_transform}, debug_info]}.
